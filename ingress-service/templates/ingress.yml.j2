apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  name: {{ ingress_name }}
  namespace: {{ Namespace }}
spec:
  rules:
    - host: {{ host_name }}
      http:
        paths:
        - backend:
            serviceName: {{ service_name }}
            servicePort: {{ Port }}
